const initialState = {
    id: null,
    steps: [],
    isFetching: true,
    error: ""
}

export const hacksStepsReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return(state)
    }
}